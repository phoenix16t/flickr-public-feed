import { shallowMount } from '@vue/test-utils'
import FeedControls from '../components/feed-controls/index.vue';

describe('component name', async () => {
  const wrapper = shallowMount(FeedControls);
  expect(wrapper).to.be(true);
});
